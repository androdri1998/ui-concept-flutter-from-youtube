import 'package:flutter/material.dart';
import 'package:community_material_icon/community_material_icon.dart';

class CustomIcons {
  static const Icon menu = Icon(
                                CommunityMaterialIcons.magnify,
                                color: Colors.white,
                                size: 30.0,
                              );
                              
  static const Icon option = Icon(
                                CommunityMaterialIcons.menu,
                                color: Colors.white,
                                size: 30.0,
                              );

  static const Icon optionMore = Icon(
                                CommunityMaterialIcons.dots_horizontal,
                                color: Colors.white,
                                size: 30.0,
                              );
}